<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Kamingo - Learn English{% endblock %}</title>

    {# Tailwind CSS CDN #}
    <script src="https://cdn.tailwindcss.com"></script>

    {# Custom Tailwind Config #}
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'programming': {
                            50: '#eff6ff',
                            100: '#dbeafe',
                            500: '#3b82f6',
                            600: '#2563eb',
                            700: '#1d4ed8',
                        },
                        'travel': {
                            50: '#f0fdf4',
                            100: '#dcfce7',
                            500: '#22c55e',
                            600: '#16a34a',
                            700: '#15803d',
                        }
                    }
                }
            }
        }
    </script>

    {% block stylesheets %}
    {% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body class="bg-gray-50 min-h-screen">
    {# Navbar - improved with better mobile support #}
    <nav class="bg-gradient-to-r from-white to-gray-50 shadow-md border-b-2 border-gray-200 sticky top-0 z-50 backdrop-blur-sm bg-opacity-95">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16 sm:h-20">
                {# Logo #}
                <div class="flex items-center">
                    <a href="{{ path('app_home') }}" class="text-2xl sm:text-3xl font-black text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 transition-all">
                        Kamingo
                    </a>
                </div>

                {# Navigation Links - responsive #}
                <div class="flex items-center space-x-2 sm:space-x-4">
                    {% if app.user %}
                        {# Logged in #}
                        <a href="{{ path('app_dashboard') }}" class="hidden sm:inline-block text-gray-700 hover:text-gray-900 hover:bg-gray-100 px-3 py-2 rounded-lg text-sm font-semibold transition-colors">
                            📊 Panel
                        </a>
                        <a href="{{ path('app_dashboard') }}" class="sm:hidden text-gray-700 hover:text-gray-900 hover:bg-gray-100 p-2 rounded-lg transition-colors">
                            <span class="text-xl">📊</span>
                        </a>
                        {% if is_granted('ROLE_ADMIN') %}
                            <a href="{{ path('admin') }}" class="hidden sm:inline-block text-blue-600 hover:text-blue-800 hover:bg-blue-50 px-3 py-2 rounded-lg text-sm font-semibold transition-colors">
                                ⚙️ Admin
                            </a>
                            <a href="{{ path('admin') }}" class="sm:hidden text-blue-600 hover:text-blue-800 hover:bg-blue-50 p-2 rounded-lg transition-colors">
                                <span class="text-xl">⚙️</span>
                            </a>
                        {% endif %}
                        <span class="hidden md:inline-block text-gray-600 text-sm font-medium px-2">
                            👤 {{ app.user.username }}
                        </span>
                        <a href="{{ path('app_logout') }}" class="bg-gradient-to-r from-gray-100 to-gray-200 hover:from-gray-200 hover:to-gray-300 text-gray-800 px-3 sm:px-5 py-2 rounded-lg text-sm font-bold transition-all shadow-sm hover:shadow-md">
                            <span class="hidden sm:inline">Wyloguj</span>
                            <span class="sm:hidden">↗️</span>
                        </a>
                    {% else %}
                        {# Not logged in #}
                        <a href="{{ path('app_home') }}" class="text-gray-700 hover:text-gray-900 hover:bg-gray-100 px-3 py-2 rounded-lg text-sm font-semibold transition-colors">
                            🏠 <span class="hidden sm:inline">Strona główna</span>
                        </a>
                        <a href="{{ path('app_login') }}" class="bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white px-4 sm:px-6 py-2 rounded-lg text-sm font-bold transition-all shadow-md hover:shadow-lg">
                            Zaloguj
                        </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>

    {# Flash Messages - improved styling #}
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        {% for type, messages in app.flashes %}
            {% for message in messages %}
                <div class="rounded-xl p-4 mb-4 shadow-lg border-2 transition-all {% if type == 'success' %}bg-gradient-to-r from-green-50 to-emerald-50 border-green-300{% elseif type == 'error' %}bg-gradient-to-r from-red-50 to-rose-50 border-red-300{% elseif type == 'info' %}bg-gradient-to-r from-blue-50 to-indigo-50 border-blue-300{% else %}bg-gradient-to-r from-gray-50 to-slate-50 border-gray-300{% endif %}">
                    <div class="flex items-center">
                        <div class="flex-shrink-0">
                            {% if type == 'success' %}
                                <span class="text-2xl">✅</span>
                            {% elseif type == 'error' %}
                                <span class="text-2xl">❌</span>
                            {% elseif type == 'info' %}
                                <span class="text-2xl">ℹ️</span>
                            {% else %}
                                <span class="text-2xl">💬</span>
                            {% endif %}
                        </div>
                        <div class="ml-4 flex-1">
                            <p class="text-sm font-medium {% if type == 'success' %}text-green-900{% elseif type == 'error' %}text-red-900{% elseif type == 'info' %}text-blue-900{% else %}text-gray-900{% endif %}">
                                {{ message }}
                            </p>
                        </div>
                    </div>
                </div>
            {% endfor %}
        {% endfor %}
    </div>

    {# Main Content #}
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {% block body %}{% endblock %}
    </main>

    {# Footer - improved styling #}
    <footer class="bg-gradient-to-r from-gray-50 to-gray-100 border-t-2 border-gray-200 mt-16">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            <div class="text-center">
                <p class="text-gray-700 font-semibold mb-2">
                    Kamingo - Nauka angielskiego z AI 🚀
                </p>
                <p class="text-gray-500 text-sm">
                    &copy; 2025 • Powered by Symfony & Claude AI
                </p>
            </div>
        </div>
    </footer>
</body>
</html>
